import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'shop.aircart.AirCart',
  appName: 'AirCart',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
